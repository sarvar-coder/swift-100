// 1. Reverse a string **without using built-in reverse**

func reverse(str: String) -> String {
    var buffer = Array(str)
    
    var left = 0
    var right = buffer.count - 1
    
    while left <= right {
        buffer
    }
    return reversed
}

print(reverse(str: "sarvar"))
print(reverse(str: "nasiba"))
print(reverse(str: "nozima"))


